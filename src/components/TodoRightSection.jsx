import React from 'react';

const TodoRightSection = (children) => {
    return (
        <div className="todo__right-section">
            {children}
        </div>
    );
};

export default TodoRightSection;